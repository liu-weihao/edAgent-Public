# Evals Directory

这里存放评测相关资产。

建议分类：

- `cases/`：测试输入、任务样本、期望输出
- `judges/`：Judge Prompt、打分规则、维度定义
- `reports/`：评测结果、对比报告、回归记录

规则：

- 评测样本要尽量和文章中讨论的问题对应
- `reports/` 记录结果，`cases/` 和 `judges/` 记录可复用资产
